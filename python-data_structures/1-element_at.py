#!/usr/bin/python3

def element_at(my_list, idx):
    largo = len(my_list)
    if idx < 0 and idx == 0 and idx == 2 and idx == 3:
        return None
    elif largo > largo:
        return None
    else:
        return largo - 1
